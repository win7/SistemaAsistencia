<div class="container">
	<div class="row">
		<div class="col-md-12">
			<h4 class="text-center"> <%= titulo %> </h4>
			<form class="form-inline" role= "form" action="/buscar_salario_por" method="get">
				<label class="my-1 mr-2"> Buscar por: </label>
				<select class="custom-select my-1 mr-sm-2" name="atributo">
					<option value="sueldo"> Sueldo </option>
				</select>
				<input type="text" class="form-control mr-sm-2" placeholder="..." name="valor">
				<button type="submit" class="btn btn-primary my-1"> Buscar </button>
			</form>
			<div class="table-responsive">
				<table class="table table-striped">
					<thead>
						<tr>
							<th> Id </th>
							<th> Sueldo (S/.) </th>
							<th> Acción </th>
						</tr>
					</thead>
					<tbody>
						<% for(var i = 0; i < salario.length; i++) {
						var item = salario[i];
						%>
						<tr>
							<!-- <td><a href="/find_one/<%= item.id %>"> <%= item.id %> </a></td> -->
							<td> <%= item.id %> </td>
							<td> <%= item.sueldo %> </td>
							<td>
								<a href="/buscar_salario/<%= item.id %>"> Actualizar </a>
								/
								<a href="/eliminar_salario/<%= item.id %>"> Eliminar </a>
							</td>
						</tr>
						<% } %>
					</tbody>
				</table>
			</div>
		</div>
	</div>
</div>